<?php
return array(
    'weibo.com'    => true,
    't.cn'         => true, //所有以 t.cn 结尾的域名都通过验证
    'www.weibo.cn' => true, //所有以 www.weibo.cn 结尾的域名都通过验证
    'edu.cn'       => true //所有以 edu.cn 结尾的域名都通过跳转验证
);